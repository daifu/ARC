<%= form_for payment, :url => order_payments_path(@order) do |f| %>
  <%= f.hidden_field :amount, :value => @order.total %>
  <%= f.hidden_field :popup, :value => true %>
  <%= f.submit 'Pay' %>
<% end %>